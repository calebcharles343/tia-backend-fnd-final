import React from "react";
import ProductsContainer from "../ui/ProductsContainer.tsx";

const Home: React.FC = () => {
  return (
    <div>
      <ProductsContainer />
    </div>
  );
};

export default Home;
